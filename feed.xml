<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://domenec-madrid.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://domenec-madrid.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-08-15T11:10:49+00:00</updated><id>https://domenec-madrid.github.io/feed.xml</id><title type="html">blank</title><subtitle>Personal website of Domènec Madrid — PhD researcher at UAB specializing in Bitcoin, cryptography, privacy and distributed systems. </subtitle><entry><title type="html">Modifying the bdk_wallet Balance API for My Summer of Bitcoin Project</title><link href="https://domenec-madrid.github.io/blog/2026/bdk-wallet-balance-api/" rel="alternate" type="text/html" title="Modifying the bdk_wallet Balance API for My Summer of Bitcoin Project"/><published>2026-08-14T00:00:00+00:00</published><updated>2026-08-14T00:00:00+00:00</updated><id>https://domenec-madrid.github.io/blog/2026/bdk-wallet-balance-api</id><content type="html" xml:base="https://domenec-madrid.github.io/blog/2026/bdk-wallet-balance-api/"><![CDATA[<p>Let me start with a brief story in this post.</p> <p>Imagine you want to buy a Coke in a supermarket, one of those that accepts bitcoin as payment. You just started using a wallet built on the BDK libraries, and half an hour ago someone sent you a few thousand sats. The transaction is still unconfirmed, but the balance says you can spend it.</p> <p>So you grab the bottle, walk to the till, scan the QR code and… the payment fails. Not enough funds.</p> <p>What happened in between is that the sender replaced their transaction with a higher-fee one (<a href="https://bitcoinops.org/en/topics/replace-by-fee/">RBF</a>) that no longer pays you anything. The original never confirmed, so the coin you were about to spend simply never existed. The supermarket is fine, the network is fine, the sender did nothing the protocol forbids. The only thing that failed is how your wallet classified that coin.</p> <p>Which brings us to the most basic thing a wallet does: telling you your balance. It sounds trivial, but <em>“how much do I have?”</em> doesn’t have a single answer, and every wallet draws the line somewhere different. <a href="https://bitcoindevkit.org">BDK</a> chooses to split it into four buckets, and deciding which coin belongs to which turns out to be surprisingly subtle.</p> <p>This summer, as part of <a href="https://www.summerofbitcoin.org/">Summer of Bitcoin</a>, I’ve been working on exactly that: fixing how BDK decides which of your unconfirmed funds you can actually trust. The work closes two long-standing bugs in bdk_wallet:</p> <ul> <li><a href="https://github.com/bitcoindevkit/bdk_wallet/issues/16"><strong>#16</strong></a> — the wallet trusted any unconfirmed coin that landed on its <strong>change</strong> keychain. But nothing stops a stranger from paying to a change address they spotted on-chain, and that money was counted as trusted even though the sender could still double-spend it.</li> <li><a href="https://github.com/bitcoindevkit/bdk_wallet/issues/273"><strong>#273</strong></a> — the mirror image. Consolidating your own coins into one of your <strong>receive</strong> addresses was marked untrusted, while the exact same transaction sent to a change address was trusted. Same wallet, same coins, different bucket.</li> </ul> <p>The fix spans two pull requests, one in the chain layer (<a href="https://github.com/bitcoindevkit/bdk/pull/2246">bdk#2246</a>) and one in the wallet layer (<a href="https://github.com/bitcoindevkit/bdk_wallet/pull/431">bdk_wallet#431</a>).</p> <p>Let’s start with the basics ;)</p> <hr/> <h2 id="not-all-balance-is-equal">Not all balance is equal</h2> <p>BDK reports your balance as four separate numbers:</p> <ul> <li><strong>Confirmed</strong> — money in transactions that are already in a block.</li> <li><strong>Immature</strong> — recently mined coins that the protocol won’t let you spend yet, since a mining reward has to wait 100 blocks.</li> <li><strong>Trusted pending</strong> — unconfirmed money the wallet is fairly sure will go through.</li> <li><strong>Untrusted pending</strong> — unconfirmed money that someone else could still pull back.</li> </ul> <p>Other wallets slice this differently, some show a single number, some only separate confirmed from unconfirmed, but the last two buckets are the interesting ones here.</p> <p><em>Pending</em> means the transaction isn’t in a block yet, so it could still be replaced or double-spent before it confirms.</p> <hr/> <h2 id="where-the-old-logic-went-wrong">Where the old logic went wrong</h2> <p>The previous implementation decided trust by looking at the <strong>keychain</strong> the money landed on. The whole rule was one closure:</p> <div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">|</span><span class="o">&amp;</span><span class="p">(</span><span class="n">k</span><span class="p">,</span> <span class="n">_</span><span class="p">),</span> <span class="n">_</span><span class="p">|</span> <span class="n">k</span> <span class="o">==</span> <span class="nn">KeychainKind</span><span class="p">::</span><span class="n">Internal</span>
</code></pre></div></div> <p>In other words: <em>“did this land on a change address? then trust it.”</em> The reasoning being that change addresses are where your own transactions send money back to you.</p> <p>That sounds reasonable, but it’s asking the wrong question. Whether you can trust an unconfirmed coin has nothing to do with which of your addresses it landed on. It depends on <strong>where the money came from</strong>, and the two bugs are the two ways that assumption breaks:</p> <ul> <li>Change addresses aren’t private. Anyone watching the chain can spot yours and pay to one, and the rule waved that payment straight through (<strong>#16</strong>).</li> <li>Your own transactions don’t always pay to change. Consolidate into a receive address and the rule flagged your own money as if a stranger might snatch it back (<strong>#273</strong>).</li> </ul> <hr/> <h2 id="ancestry-based-solution">Ancestry based solution</h2> <p>So the wallet stops asking <em>“whose address did this land on?”</em> and starts asking <em>“whose coins paid for it, all the way back?”</em></p> <table> <thead> <tr> <th>Case</th> <th>Condition</th> <th>Why</th> </tr> </thead> <tbody> <tr> <td><strong>Trusted</strong></td> <td>The coin’s entire unconfirmed history only spends coins you already own</td> <td>You made every one of those transactions, so no outsider can replace them</td> </tr> <tr> <td><strong>Untrusted</strong></td> <td>Somewhere in that unconfirmed history it pulls in coins that aren’t yours</td> <td>Whoever controls those coins can still replace the transaction</td> </tr> <tr> <td><strong>Unknown</strong></td> <td>The wallet can’t see far enough back, e.g. a parent transaction it never downloaded</td> <td>A history you can’t verify isn’t a safe one, so it falls back to untrusted</td> </tr> </tbody> </table> <p>The coin from the supermarket now lands in the middle row, which is exactly the honest answer, and the one that would have saved you the walk to the till.</p> <p>Answering that question takes both layers of the library.</p> <h3 id="in-bdk_chain">In <code class="language-plaintext highlighter-rouge">bdk_chain</code></h3> <p>I added <code class="language-plaintext highlighter-rouge">classify_outpoints</code>, which labels every one of your coins with its spend eligibility: <em>settled</em>, <em>immature</em>, <em>trusted pending</em> or <em>untrusted pending</em>. It walks back through the transactions that funded a coin and stops as soon as it reaches something confirmed or something tainted, remembering what it has already seen so shared history is never walked twice. <code class="language-plaintext highlighter-rouge">balance</code> then becomes a thin fold over this classification.</p> <p>Crucially, the chain layer doesn’t decide what counts as tainted, or as final. It takes two predicates, one per concern:</p> <ul> <li><code class="language-plaintext highlighter-rouge">does_taint(&amp;tx)</code> — should this transaction be considered tainted?</li> <li><code class="language-plaintext highlighter-rouge">is_settled(&amp;pos)</code> — do we consider this chain position final?</li> </ul> <h3 id="in-bdk_wallet">In <code class="language-plaintext highlighter-rouge">bdk_wallet</code></h3> <p>The wallet supplies the first one, <em>“taint a transaction if any of its inputs spends an output I don’t own”</em>. That single rule, applied along the walk, is what produces the whole table above. The wallet never has to reason about ancestry itself, and it never has to mention keychains again.</p> <p>It also folds <code class="language-plaintext highlighter-rouge">classify_outpoints</code> directly instead of calling the chain’s <code class="language-plaintext highlighter-rouge">balance</code>. Same result, but it leaves room to layer wallet-specific categories (locked or reserved coins, for instance) on top without touching bdk_chain.</p> <h3 id="maturity-is-not-settledness">Maturity is not settledness</h3> <p>Along the way I also separated two ideas that had been tangled together:</p> <ul> <li><strong>Maturity</strong> — I think this one is clear.</li> <li><strong>Settledness</strong> — how many confirmations <em>you personally</em> want before treating money as final. Entirely your call.</li> </ul> <p>They used to be the same check, and now they aren’t, which is what makes the new option to require a minimum number of confirmations possible.</p> <hr/> <h2 id="memories-from-a-newbie-in-open-source-contribution">Memories from a newbie in open-source contribution</h2> <p>Maybe the biggest lesson of the summer is that a good fix is rarely the <em>first</em> fix. This one went through several complete redesigns in review with the BDK maintainers and other contributors.</p> <p>It started as a self-contained walk inside the wallet, then took over an earlier attempt in the chain layer (<a href="https://github.com/bitcoindevkit/bdk/pull/2235">bdk#2235</a>), and through discussion it ended up as a smaller, faster, memoized walk that only ever inspects your unconfirmed coins and their ancestors, so it doesn’t get slower as your wallet history grows.</p> <p>A lot of the value came from other people poking holes in the approach. Performance concerns, edge cases like missing history, and questions about what “trust” should even mean. Contributing to open source is much less about writing the patch than about defending, breaking and rebuilding it in public.</p> <hr/> <h2 id="where-it-stands">Where it stands</h2> <p>Both pull requests are still open and moving through review: <a href="https://github.com/bitcoindevkit/bdk/pull/2246">bdk#2246</a> in the chain layer and <a href="https://github.com/bitcoindevkit/bdk_wallet/pull/431">bdk_wallet#431</a> in the wallet. There are threads still to settle, naming, and exactly how the minimum confirmations should behave, but the core idea is in place:</p> <blockquote> <p>A wallet should trust a coin based on where it came from, not on where it landed.</p> </blockquote> <p>Stay tuned, I’ll update this post once everything is merged!</p> <p><sub><em>Thanks to <a href="https://github.com/nymius">Nymius</a> for mentoring me through all of this, and to <a href="https://github.com/evanlinjin">Evan</a> for the groundwork in <a href="https://github.com/bitcoindevkit/bdk/pull/2235">bdk#2235</a> and for sitting through round after round of review.</em></sub></p>]]></content><author><name></name></author><category term="Bitcoin"/><category term="BDK"/><category term="Rust"/><category term="Wallets"/><category term="Summer of Bitcoin"/><category term="Open Source"/><summary type="html"><![CDATA[How I changed the way bdk_wallet decides which unconfirmed coins count as trusted, by following where the money came from instead of which keychain it landed on.]]></summary></entry><entry><title type="html">Is Hardware Wallet Fingerprinting Even Possible?</title><link href="https://domenec-madrid.github.io/blog/2026/hww-fingerprinting/" rel="alternate" type="text/html" title="Is Hardware Wallet Fingerprinting Even Possible?"/><published>2026-04-01T00:00:00+00:00</published><updated>2026-04-01T00:00:00+00:00</updated><id>https://domenec-madrid.github.io/blog/2026/hww-fingerprinting</id><content type="html" xml:base="https://domenec-madrid.github.io/blog/2026/hww-fingerprinting/"><![CDATA[<p>Every Bitcoin transaction contains one or more digital signatures. These signatures authorize the spending of funds, but they can also inadvertently reveal information about how or “who” have generated them.</p> <p>Let’s start with a brief description ;)</p> <p><strong>Wallet fingerprinting</strong> is the practice of identifying which wallet, created a particular transaction. Software wallets are already relatively well studied in this area: they differ in how they select UTXOs, order inputs and outputs, estimate fees, etc. By the other hand, Hardware wallets however, have received almost no attention, largely because it’s assumed they’re all the same: isolated signing devices that just produce a deterministic signature and nothing more.</p> <p>Well, that assumption turns out to be only partly true.</p> <hr/> <h3 id="whats-actually-in-a-signature">What’s actually in a signature?</h3> <p>To understand fingerprinting on those devices, you need to understand what a Bitcoin signature contains and how it’s encoded.</p> <h4 id="ecdsa-legacy-and-segwit">ECDSA (Legacy and SegWit)</h4> <p>ECDSA signatures produce a pair of values $(r, s)$. The $r$ value is derived from a randomly chosen nonce $k$: specifically, $r = (kG)_x \mod n$.<br/> The $s$ value encodes the actual authorization using the private key: $s = k⁻1(z+dr) \mod n$.</p> <p>These values are encoded in <strong>DER format</strong>, which has a quirk: if either $r$ or $s$ starts with a byte of <code class="language-plaintext highlighter-rouge">0x80</code> or higher, a <code class="language-plaintext highlighter-rouge">0x00</code> padding byte must be prepended to avoid the value being interpreted as negative (DER uses two’s complement). This means a signature can be anywhere from 70 to 73 bytes long.</p> <p>Since <a href="https://github.com/bitcoin/bips/blob/master/bip-0062.mediawiki">BIP-62</a>, the $s$ value is always normalized to the lower half of the curve, capping signatures at 72 bytes. But $r$ is still variable: roughly 50% of the time, $r$ starts with a high byte and needs that extra <code class="language-plaintext highlighter-rouge">0x00</code>.</p> <h4 id="schnorr-taproot">Schnorr (Taproot)</h4> <p>Schnorr signatures always encode to exactly <strong>64 bytes</strong>, 32 for the $x$-coordinate of the nonce point $R$, and 32 for $s$. No DER encoding, no variable length, no malleability.</p> <hr/> <div class="card my-4 border" id="ecdsa-demo"> <div class="card-body"> <h6 class="card-title fw-bold mb-1">ECDSA vs Schnorr — signature length visualizer</h6> <p class="card-text text-muted mb-3" style="font-size:0.85rem"> Random values simulate the nonce-derived $r$ and the BIP-62-normalized $s$. Hover any byte for its role. </p> <div class="d-flex align-items-center gap-3 flex-wrap mb-3"> <button class="btn btn-sm btn-primary" id="ecdsa-sign-btn">Sign</button> <button class="btn btn-sm btn-outline-secondary" id="ecdsa-sign-many-btn">Sign 100×</button> <div class="form-check form-switch mb-0"> <input class="form-check-input" type="checkbox" id="ecdsa-lowr-toggle"/> <label class="form-check-label small" for="ecdsa-lowr-toggle">Low-<em>r</em> grinding</label> </div> </div> <div id="ecdsa-output" style="display:none"> <div class="d-flex gap-3 mb-3 flex-wrap"> <div class="text-center"> <div id="ecdsa-badge-len" class="badge fs-6">?? bytes</div> <div class="small text-muted mt-1">ECDSA size</div> </div> <div class="text-center" id="ecdsa-attempts-block" style="display:none"> <div id="ecdsa-badge-attempts" class="badge bg-secondary fs-6">? attempt</div> <div class="small text-muted mt-1">grinding rounds</div> </div> <div class="text-center"> <div class="badge bg-info fs-6" style="color:#000">64 bytes</div> <div class="small text-muted mt-1">Schnorr size</div> </div> </div> <div class="mb-2"> <div class="small fw-bold mb-1">ECDSA — DER encoding</div> <div id="ecdsa-der-viz" class="font-monospace" style="word-break:break-all;line-height:2.1;font-size:0.78rem"></div> <div class="mt-2 d-flex flex-wrap gap-1" style="font-size:0.75rem"> <span class="badge" style="background:#6c757d">structure</span> <span class="badge" style="background:#0d6efd">r bytes</span> <span class="badge" style="background:#dc3545">0x00 padding</span> <span class="badge" style="background:#198754">s bytes</span> <span class="badge" style="background:#6f42c1">SIGHASH</span> </div> </div> <div class="mt-3"> <div class="small fw-bold mb-1">Schnorr (Taproot) — always 64 bytes, no DER</div> <div id="ecdsa-schnorr-viz" class="font-monospace" style="word-break:break-all;line-height:2.1;font-size:0.78rem"></div> <div class="mt-2 d-flex flex-wrap gap-1" style="font-size:0.75rem"> <span class="badge" style="background:#0d6efd">R x-coord (32 B)</span> <span class="badge" style="background:#198754">s (32 B)</span> </div> </div> <div id="ecdsa-many-stats" style="background:var(--global-code-bg-color)" class="mt-3 p-2 rounded"> <div class="small fw-bold">Last 100 signatures:</div> <div class="d-flex gap-3 mt-1 small"> <span><strong id="ecdsa-c71">0</strong> × 71 bytes</span> <span><strong id="ecdsa-c72">0</strong> × 72 bytes</span> </div> </div> </div> </div> </div> <script>
(function () {
  function randBytes(n) {
    const a = new Uint8Array(n);
    crypto.getRandomValues(a);
    return a;
  }

  function signECDSA(grind) {
    let r, attempts = 0;
    do {
      r = randBytes(32);
      attempts++;
    } while (grind && r[0] >= 0x80 && attempts < 500);

    // BIP-62: low-s ⟹ s[0] always < 0x80
    const s = randBytes(32);
    s[0] = s[0] & 0x7f;
    if (s[0] === 0) s[0] = 0x01;

    const needsPad = r[0] >= 0x80;
    const rLen = needsPad ? 33 : 32;
    // structure: 0x30 totalLen 0x02 rLen [pad] r 0x02 0x20 s 0x01
    const totalLen = 2 + rLen + 2 + 32;
    const sigLen = 1 + 1 + totalLen + 1; // +1 sighash

    return { r, s, needsPad, rLen, totalLen, sigLen, attempts };
  }

  function byte(hex, title, bg, fg) {
    fg = fg || '#fff';
    return '<span title="' + title + '" style="display:inline-block;background:' + bg +
      ';color:' + fg + ';padding:1px 5px;border-radius:3px;margin:1px 1px;cursor:default;font-size:0.78rem">' + hex + '</span>';
  }

  function renderDER(sig) {
    const { r, s, needsPad, rLen, totalLen } = sig;
    let h = '';
    h += byte('30', 'SEQUENCE tag', '#6c757d');
    h += byte(totalLen.toString(16).padStart(2, '0'), 'total length: ' + totalLen + ' bytes', '#6c757d');
    h += byte('02', 'INTEGER tag (r)', '#0d6efd');
    h += byte(rLen.toString(16).padStart(2, '0'), 'r length: ' + rLen + ' bytes', '#0d6efd');
    if (needsPad) h += byte('00', 'padding: r[0] ≥ 0x80, two\'s-complement sign bit', '#dc3545');
    Array.from(r).forEach(function (b, i) {
      h += byte(b.toString(16).padStart(2, '0'), 'r[' + i + ']' + (i === 0 && needsPad ? ' — would be negative without padding' : ''), '#0d6efd');
    });
    h += byte('02', 'INTEGER tag (s)', '#198754');
    h += byte('20', 's length: 32 bytes (BIP-62 low-s ⟹ never needs padding)', '#198754');
    Array.from(s).forEach(function (b, i) {
      h += byte(b.toString(16).padStart(2, '0'), 's[' + i + ']', '#198754');
    });
    h += byte('01', 'SIGHASH_ALL', '#6f42c1');
    return h;
  }

  function renderSchnorr() {
    const R = randBytes(32);
    const s = randBytes(32);
    let h = '';
    Array.from(R).forEach(function (b, i) {
      h += byte(b.toString(16).padStart(2, '0'), 'R[' + i + '] — nonce point x-coordinate', '#0d6efd');
    });
    Array.from(s).forEach(function (b, i) {
      h += byte(b.toString(16).padStart(2, '0'), 's[' + i + ']', '#198754');
    });
    return h;
  }

  function updateUI(sig) {
    const grind = document.getElementById('ecdsa-lowr-toggle').checked;
    document.getElementById('ecdsa-badge-len').textContent = sig.sigLen + ' bytes';
    document.getElementById('ecdsa-badge-len').className = 'badge fs-6 ' + (sig.sigLen === 71 ? 'bg-success' : 'bg-warning');
    document.getElementById('ecdsa-badge-len').style.color = sig.sigLen === 71 ? '' : '#000';
    const attBlock = document.getElementById('ecdsa-attempts-block');
    if (grind) {
      attBlock.style.display = '';
      document.getElementById('ecdsa-badge-attempts').textContent = sig.attempts + ' attempt' + (sig.attempts !== 1 ? 's' : '');
    } else {
      attBlock.style.display = 'none';
    }
    document.getElementById('ecdsa-der-viz').innerHTML = renderDER(sig);
    document.getElementById('ecdsa-schnorr-viz').innerHTML = renderSchnorr();
    document.getElementById('ecdsa-output').style.display = '';
  }

  document.addEventListener('DOMContentLoaded', function () {
    document.getElementById('ecdsa-sign-btn').addEventListener('click', function () {
      const sig = signECDSA(document.getElementById('ecdsa-lowr-toggle').checked);
      updateUI(sig);
      document.getElementById('ecdsa-many-stats').style.display = 'none';
    });

    document.getElementById('ecdsa-sign-many-btn').addEventListener('click', function () {
      const grind = document.getElementById('ecdsa-lowr-toggle').checked;
      let c71 = 0, c72 = 0;
      for (let i = 0; i < 100; i++) {
        const s = signECDSA(grind);
        s.sigLen === 71 ? c71++ : c72++;
      }
      updateUI(signECDSA(grind));
      document.getElementById('ecdsa-c71').textContent = c71;
      document.getElementById('ecdsa-c72').textContent = c72;
      document.getElementById('ecdsa-many-stats').style.display = '';
    });
  });
})();
</script> <hr/> <h3 id="the-fingerprinting-vectors">The fingerprinting vectors</h3> <p>Given the above, what can actually be used to fingerprint a hardware wallet?</p> <h4 id="1-low-r-grinding">1. Low-$r$ grinding</h4> <p>Some wallets iterate over candidate nonces until $r$ falls in the low range of the curve (i.e., doesn’t need the leading <code class="language-plaintext highlighter-rouge">0x00</code>). This “low-$r$ grinding” produces signatures that are always 71 bytes instead of the usual 71–72.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/hww-fp/ecdsa_len-480.webp 480w,/assets/img/posts/hww-fp/ecdsa_len-800.webp 800w,/assets/img/posts/hww-fp/ecdsa_len-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/posts/hww-fp/ecdsa_len.png" class="img-fluid w-75 d-block mx-auto rounded z-depth-1" width="100%" height="auto" alt="ECDSA signature length distribution over time" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">ECDSA signature length over time. The shift toward 71-byte signatures around October 2018 marks the adoption of low-r grinding in Bitcoin Core v0.17.0. Source: mainnet.observer</figcaption> </figure> <p>The fingerprinting logic here is asymmetric:</p> <ul> <li>A <strong>72-byte signature</strong> means the wallet does <em>not</em> implement low-$r$ grinding.</li> <li>A <strong>71-byte signature</strong> doesn’t prove grinding, it could just be a lucky nonce.</li> </ul> <p>So… it is a probabilistic fingerprint.</p> <h4 id="2-sighash-type">2. SIGHASH type</h4> <p>Every ECDSA signature ends with a one-byte SIGHASH flag that defines which parts of the transaction are covered by the signature.</p> <p>If a transaction uses any SIGHASH type other than <code class="language-plaintext highlighter-rouge">ALL</code>, it immediately narrows down which wallets could have produced it, since most hardware wallets only support <code class="language-plaintext highlighter-rouge">SIGHASH_ALL</code>.</p> <h4 id="3-nonce-determinism-rfc-6979">3. Nonce determinism (RFC 6979)</h4> <p><a href="https://datatracker.ietf.org/doc/html/rfc6979">RFC 6979</a> defines a way to generate the nonce $k$ deterministically from the private key and the message, rather than randomly. This means signing the same PSBT twice with the same key should produce identical signatures.</p> <p>In theory, this could be used for fingerprinting, you just have to sign the same transaction twice and compare. If the signatures are identical, the wallets uses RFC 6979. But in practice, <strong>this is only detectable in a lab setting</strong>, you can’t broadcast two conflicting transactions, so an external observer on the blockchain cannot use this trait.</p> <p>During our experiments, we observed some of the wallets implementing RFC 6979 were differing on signature values. We concluded it was related to <em>low R grinding</em> and the number of RFC 6979 rounds they performed to obtain a low-R value.</p> <hr/> <h3 id="building-a-test-environment">Building a test environment</h3> <p>One of the practical problems in this research was that some of the wallets we wanted to test <strong>only work on Bitcoin mainnet</strong>. So we built our own main-net, a parallel Bitcoin mainnet that shares the same genesis block as the real network but runs in complete isolation. See how to replicate it <a href="https://domenec-madrid.github.io/blog/2025/byom/">here :)</a></p> <p>Although this environment is not necessary for signature creation and observation, we needed it to verify whether signatures generated with different sighashes were valid on mainnet.</p> <p>In any case, find the full setup available at <a href="https://github.com/Dmenec/BTC-Labnet">BTC-Labnet</a>.</p> <hr/> <h3 id="results">Results</h3> <p>We tested ten devices, all initialized with the same BIP-39 seed phrase, all connected to <a href="https://sparrowwallet.com/">Sparrow Wallet</a> (or their official companion app where Sparrow wasn’t supported). For each wallet, we signed the same PSBT multiple times and observed the signatures.</p> <p>The results clustered into three groups:</p> <table> <thead> <tr> <th>Group</th> <th>Behavior</th> </tr> </thead> <tbody> <tr> <td>RFC 6979, no low-$r$ grinding</td> <td>Deterministic signatures, 71–72 byte range</td> </tr> <tr> <td>RFC 6979 + low-$r$ grinding</td> <td>Deterministic, always 71 bytes</td> </tr> <tr> <td>Anti-exfil, no low-$r$ grinding</td> <td>Non-deterministic by design, 71–72 byte range</td> </tr> </tbody> </table> <p><small> In case you’ve haven’t heard of <a href="https://medium.com/blockstream/anti-exfil-stopping-key-exfiltration-589f02facc2e">anti-exfil</a>.</small></p> <p>Wallets like <strong>BitBox02</strong> and <strong>Jade</strong> deliberately introduce external randomness into nonce generation to prevent covert channel attacks (like Dark Skippy, which I’ve written about <a href="/blog/2025/cvca/">before</a>). This makes their signatures non-deterministic, and therefore distinguishable from the rest.</p> <p>On SIGHASH support, the picture was stark. Most wallets refused to sign with anything other than <code class="language-plaintext highlighter-rouge">SIGHASH_ALL</code>. Only <strong>Ledger Nano S+</strong> and <strong>Keystone 3 Pro</strong> accepted the full range of SIGHASH types, making them immediately identifiable when those types appear in the wild.</p> <p>Here’s the summary:</p> <table> <thead> <tr> <th>Wallet</th> <th style="text-align: center">Low-$r$</th> <th style="text-align: center">NONE</th> <th style="text-align: center">SINGLE</th> <th style="text-align: center">ANYONECANPAY variants</th> <th style="text-align: center">Cluster</th> </tr> </thead> <tbody> <tr> <td>Ledger Nano S Plus (Bitcoin App 2.3.0)</td> <td style="text-align: center">✗</td> <td style="text-align: center">✓</td> <td style="text-align: center">✓</td> <td style="text-align: center">✓</td> <td style="text-align: center">A</td> </tr> <tr> <td>BitBox02 (Multi 9.21.0)</td> <td style="text-align: center">✗</td> <td style="text-align: center">✗</td> <td style="text-align: center">✗</td> <td style="text-align: center">✗</td> <td style="text-align: center">B</td> </tr> <tr> <td>Foundation Passport (2.3.5)</td> <td style="text-align: center">✗</td> <td style="text-align: center">✗</td> <td style="text-align: center">✗</td> <td style="text-align: center">✗</td> <td style="text-align: center">B</td> </tr> <tr> <td>Trezor Model T Safe 3 (2.8.7)</td> <td style="text-align: center">✗</td> <td style="text-align: center">✗</td> <td style="text-align: center">✗</td> <td style="text-align: center">✗</td> <td style="text-align: center">B</td> </tr> <tr> <td>Trezor Model T Safe 5 (2.8.7)</td> <td style="text-align: center">✗</td> <td style="text-align: center">✗</td> <td style="text-align: center">✗</td> <td style="text-align: center">✗</td> <td style="text-align: center">B</td> </tr> <tr> <td>Coldcard Mk4 (5.2.0)</td> <td style="text-align: center">✓</td> <td style="text-align: center">✗</td> <td style="text-align: center">✗</td> <td style="text-align: center">✗</td> <td style="text-align: center">C</td> </tr> <tr> <td>Blockstream Jade (1.0.35)</td> <td style="text-align: center">✓</td> <td style="text-align: center">✗</td> <td style="text-align: center">✗</td> <td style="text-align: center">✗</td> <td style="text-align: center">C</td> </tr> <tr> <td>KeepKey (7.9.1)</td> <td style="text-align: center">✗</td> <td style="text-align: center">✗</td> <td style="text-align: center">✗</td> <td style="text-align: center">✗</td> <td style="text-align: center">B</td> </tr> <tr> <td>Keystone 3 Pro (BTC 1.1.4)</td> <td style="text-align: center">✗</td> <td style="text-align: center">✓</td> <td style="text-align: center">✓</td> <td style="text-align: center">✓</td> <td style="text-align: center">A</td> </tr> <tr> <td>SeedSigner (0.7.0)</td> <td style="text-align: center">✓</td> <td style="text-align: center">✗</td> <td style="text-align: center">✗</td> <td style="text-align: center">✗</td> <td style="text-align: center">C</td> </tr> </tbody> </table> <hr/> <h3 id="so-are-them-fingerprintable-or-not">So, are them fingerprintable or not?</h3> <p>Hardware wallets are, in general, much harder to fingerprint than software wallets.</p> <p>The main difficulty in fingerprinting them is determining whether a signature was produced by a hardware wallet rather than a software one, which is practically impossible without additional information. Even if you know that a transaction was signed using a hardware wallet, you might be able to assign it to one of the three groups of devices. However, there is still no reliable way to distinguish between individual wallets within the same group.</p> <p>Therefore, we can conclude that individually distinguishing a hardware wallet from a signature is essentially infeasible.</p>]]></content><author><name></name></author><category term="Bitcoin"/><category term="Cryptography"/><category term="Hardware Wallets"/><category term="Wallet Fingerprinting"/><category term="Signatures"/><category term="Privacy"/><summary type="html"><![CDATA[Can you tell which hardware wallet signed a Bitcoin transaction just by looking at its ECDSA or Schnorr signature? We tested ten devices and found out.]]></summary></entry><entry><title type="html">Covert-Nonce Channel Attacks on Bitcoin Hardware Wallets</title><link href="https://domenec-madrid.github.io/blog/2025/cvca/" rel="alternate" type="text/html" title="Covert-Nonce Channel Attacks on Bitcoin Hardware Wallets"/><published>2025-09-01T00:00:00+00:00</published><updated>2025-09-01T00:00:00+00:00</updated><id>https://domenec-madrid.github.io/blog/2025/cvca</id><content type="html" xml:base="https://domenec-madrid.github.io/blog/2025/cvca/"><![CDATA[<p>When we talk about self-custody and protecting our bitcoins, we understand that the seed used to generate our private keys must be completely isolated and stored securely. The best-known solutions on the market for solving this problem are hardware/cold wallets.</p> <p>These impenetrable fortresses seem perfect for this task. Isolated from the internet, protected by passwords to prevent unauthorized access, equipped with secure chips to resist physical attacks, and designed to prevent any kind of information leak. After all, the operations they perform are critical when it comes to cryptocurrency management.</p> <p>It seems impossible to attack these devices if access is completely restricted. But… what if, just like Agamemnon’s army infiltrated Troy with the famous wooden horse, an attacker managed to gain access to the hardware wallet without the user’s knowledge?</p> <hr/> <h2 id="the-dark-skippy-case">The Dark Skippy case</h2> <p>A few years ago, Lloyd Fournier, Nick Farrow, and Robin Linus discovered a vulnerability affecting probabilistic signing devices that allows private keys to be ex-filtrated secretly through the signatures they generate. This discovery was named <a href="https://darkskippy.com/">Dark Skippy</a> and exploits the random value in the signature, which in principle should be generated deterministically (<a href="https://datatracker.ietf.org/doc/html/rfc6979">RFC 6979</a>), by embedding secret information known only to the attacker within the signature.</p> <p>The attack they demonstrated is a covert channel attack using the nonce of probabilistic signatures. By modifying the device’s firmware, it is possible to alter the way signatures are generated in order to produce a predictable “random” value derived from a seed. When a transaction is made on a public network, e.g., the blockchain, the attacker can identify the transaction and, with some effort, recover the ex-filtrated critical data.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/cvca/dark-skippy-480.webp 480w,/assets/img/posts/cvca/dark-skippy-800.webp 800w,/assets/img/posts/cvca/dark-skippy-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/posts/cvca/dark-skippy.png" class="img-fluid w-75 d-block mx-auto rounded z-depth-1" width="100%" height="auto" alt="Dark Skippy covert channel attack" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Figure 1. Dark Skippy – attack schematic.</figcaption> </figure> <hr/> <h2 id="practical-example-supplychain-attack">Practical example: supply‑chain attack</h2> <p>Some of you might be thinking, <em>“Domènec, there’s an inconsistency here, you told us that hardware wallets are isolated, password‑protected and very secure; how could anyone modify the device firmware?”</em> You’re right, these attacks are genuinely difficult to carry out. Still, extremely sophisticated methods exist that can succeed.</p> <blockquote> <p><strong>For example, imagine a classic supply‑chain attack:</strong> a factory produces thousands upon thousands of these devices. One day the company executives do the math and realise the value stored on their devices is worth several times the firm’s annual profit. They hear about these kinds of attacks and decide to go ahead. They modify the firmware, ship out malicious devices, and wait for months. Time passes and users begin making transactions meanwhile the “attackers” monitor the blockchain. Gradually they recover seeds, and eventually they decide they have enough. Once they have the private keys they start moving users funds. At this point, Alice, who bought one of the company’s devices a few months earlier, checks her hot wallet<sup id="fnref:hotwallet"><a href="#fn:hotwallet" class="footnote" rel="footnote" role="doc-noteref">1</a></sup> and sees transactions she did not author, notices funds moving to unknown addresses, and discovers her wallet is completely empty. Naturally, she panics.</p> </blockquote> <p>As the previous example shows, Alice made only a couple of transactions with the compromised device and did not realise she’d been attacked until it was too late. That’s because these attacks are very hard to detect, the device continues to function correctly and produces signatures that look valid and secure to everyone except the attacker. Who, by dealing with the discrete‑log problem, can recover parts of the seed.</p> <hr/> <h2 id="a-proposed-solution-antiexfil">A proposed solution: Anti‑Exfil</h2> <p>Not everything is hopeless. Andrew Poelstra and Jonas Nick proposed a <a href="https://medium.com/blockstream/anti-exfil-stopping-key-exfiltration-589f02facc2e">way to prevent the nonce generation from depending solely on the hardware wallet</a>. Their proposal prevents this class of attacks by sharing secrets between the hot wallet and the cold wallet. Concretely, the scheme forces both participants to commit to shared secrets so that the signature’s randomness is partly derived from those secrets. That way, when a transaction is signed the hot wallet can verify that the nonce was generated correctly, and the user can be confident the hardware wallet produced the value honestly at random.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/cvca/anti-exfil-480.webp 480w,/assets/img/posts/cvca/anti-exfil-800.webp 800w,/assets/img/posts/cvca/anti-exfil-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/posts/cvca/anti-exfil.png" class="img-fluid w-75 d-block mx-auto rounded z-depth-1" width="100%" height="auto" alt="Anti‑Exfil protocol diagram" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Figure 2. Diagram of the Anti‑Exfil protocol.</figcaption> </figure> <p>Today this solution is implemented by the Jade and BitBox wallets. Each implements its own Anti‑Exfil system, which unfortunately means the protocol can only be fully validated using the corresponding hot wallet provided by the same vendor.</p> <p>However, the solution has a usability problem. As shown in Figure 2, the protocol is a two‑round exchange. In practice this is unnoticeable when the devices are physically connected by cable. Unfortunately, with air‑gapped devices you have no choice but to perform the operations twice (usually via QR codes) to complete the protocol.</p> <p>Although effective, this approach is impractical for cable‑free devices. That leaves an open research question: is it possible to design a single‑round protocol? The Discrypt group will continue researching and testing new ideas until we find a solution that is both practical and secure.</p> <p>Stay tuned, we might have some fresh news to share sooner rather than later!</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/cvca/office-480.webp 480w,/assets/img/posts/cvca/office-800.webp 800w,/assets/img/posts/cvca/office-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/posts/cvca/office.png" class="img-fluid w-75 d-block mx-auto rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="footnotes" role="doc-endnotes"> <ol> <li id="fn:hotwallet"> <p>A wallet connected to the internet. In the context we’re discussing, it should not store the seed. <a href="#fnref:hotwallet" class="reversefootnote" role="doc-backlink">&#8617;</a></p> </li> </ol> </div>]]></content><author><name></name></author><category term="Bitcoin,"/><category term="Cryptography,"/><category term="Hardware"/><category term="Wallets,"/><category term="Signatures"/><summary type="html"><![CDATA[How a compromised hardware wallet can silently leak your Bitcoin seed through the signatures it produces — and what the Anti-Exfil protocol does to stop it.]]></summary></entry><entry><title type="html">Build Your Own Bitcoin Main-net</title><link href="https://domenec-madrid.github.io/blog/2025/byom/" rel="alternate" type="text/html" title="Build Your Own Bitcoin Main-net"/><published>2025-05-01T00:00:00+00:00</published><updated>2025-05-01T00:00:00+00:00</updated><id>https://domenec-madrid.github.io/blog/2025/byom</id><content type="html" xml:base="https://domenec-madrid.github.io/blog/2025/byom/"><![CDATA[<p>During my initial research period at UAB, I began developing a replica of the real Bitcoin mainnet. While this might sound unusual—especially given the existence of testnet and regtest networks—there’s a clear motivation behind it.</p> <p>Our research team needed a controlled environment to analyse <strong>wallet fingerprints</strong> for a further study on address clustering. Since many wallets offer limited or no support for testnet or regtest, we created a full-featured, <strong>isolated mainnet</strong> that behaves like the real Bitcoin network while remaining entirely under our control.</p> <p>The implemented network is built directly from the <strong>original Bitcoin Core source code</strong>, but it operates in complete isolation from Bitcoin’s public mainnet.</p> <hr/> <h2 id="why-build-your-own-mainnet">Why build your own mainnet?</h2> <p>Creating a private Bitcoin mainnet replica opens the door to capabilities that are otherwise impossible or impractical to achieve.</p> <h3 id="benefits"><strong>Benefits</strong></h3> <ul> <li><strong>Total control over the chain</strong> <ul> <li>Execute tests in a “distributed‑nature” environment without relying on real distributed infrastructure.</li> <li>Perform large‑scale transaction testing.</li> </ul> </li> <li><strong>Easy deployment</strong>, since every node runs inside a Docker container.</li> <li><strong>Compatible with software and hardware that only operate in mainnet environments.</strong></li> <li><strong>Zero‑cost transactions</strong>, enabling unlimited experimentation.</li> </ul> <hr/> <h2 id="requirements">Requirements</h2> <p>To ensure that the chain works exactly as expected, two constraints must be respected:</p> <ul> <li><strong>SegWit and Taproot must be enabled from the genesis block.</strong><br/> Their activation heights influence transaction validity; using these features before their expected height would cause nodes to reject the blocks.</li> <li><strong>The network must remain <em>fully isolated</em> from the internet</strong> to prevent accidental synchronization with the real Bitcoin blockchain.</li> </ul> <hr/> <h2 id="architecture">Architecture</h2> <p>We define the network with three critical node types:</p> <ul> <li><strong>Full Node</strong>: Ensures consensus on the chain.</li> <li><strong>Router Node</strong>: Manages peer connectivity.</li> <li><strong>Miner Node</strong>: Mines a block whenever a transaction is sent into the network.</li> </ul> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/byom/nodes-480.webp 480w,/assets/img/posts/byom/nodes-800.webp 800w,/assets/img/posts/byom/nodes-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/posts/byom/nodes.png" class="img-fluid w-50 d-block mx-auto rounded z-depth-1" width="100%" height="auto" alt="Node architecture diagram" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Figure 1. Architecture of the node setup.</figcaption> </figure> <p>Each node is built into a custom <strong>Ubuntu-based Docker image containing Bitcoin Core 24.0.1</strong>, resulting in an image size of approximately <strong>5.11 GB</strong>.<br/> The associated <code class="language-plaintext highlighter-rouge">Dockerfile</code> clones Bitcoin Core, applies some modifications, compiles it, and configures the node for execution.</p> <h3 id="modified-bitcoin-core-files">Modified Bitcoin Core files</h3> <blockquote> <p>The two required modifications are:</p> <ul> <li><strong>chainparams.cpp</strong> — SegWit and Taproot are activated at genesis.</li> <li><strong>mining.cpp</strong> — Removes initial block validation checks to allow mining even when the node is not synchronized.</li> </ul> </blockquote> <p>An <strong>entrypoint script</strong> ensures the service starts with minimal permissions and only launches Docker if the <code class="language-plaintext highlighter-rouge">bitcoind</code> daemon is healthy.</p> <hr/> <h2 id="configuring-bitcoinconf">Configuring <code class="language-plaintext highlighter-rouge">bitcoin.conf</code></h2> <p>The <code class="language-plaintext highlighter-rouge">bitcoin.conf</code> file defines how each Bitcoin node behaves within the replica network.</p> <p>To achieve the expected behavior, you should configure at least:</p> <ul> <li><strong>rpcallowip</strong> — Restrict RPC access to the Docker network or a specific IP.</li> <li><strong>paytxfee</strong> — Set a static fee (e.g., <code class="language-plaintext highlighter-rouge">0.0001</code>) since fee estimation will fail on an empty chain.</li> <li><strong>minimumchainwork = 0</strong> — As the network starts fully fresh.</li> <li><strong>txindex = 1</strong> — Optional, but useful for exploring transactions.</li> </ul> <blockquote> <p>💭 You can experiment with <code class="language-plaintext highlighter-rouge">bitcoin.conf</code> settings <a href="https://jlopp.github.io/bitcoin-core-config-generator/">here :)</a></p> </blockquote> <hr/> <h2 id="network-setup">Network setup</h2> <p>Each node is deployed inside a <strong>Docker network</strong>, ensuring full isolation from external peers.<br/> Connections between nodes rely on <a href="https://developer.bitcoin.org/reference/rpc/addnode.html"><code class="language-plaintext highlighter-rouge">addnode</code></a>, linking all peers through the router node.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/byom/network-480.webp 480w,/assets/img/posts/byom/network-800.webp 800w,/assets/img/posts/byom/network-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/posts/byom/network.png" class="img-fluid w-75 d-block mx-auto rounded z-depth-1" width="100%" height="auto" alt="Network architecture" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Figure 2. Network configuration for the main-net replica.</figcaption> </figure> <p>You can connect external software wallets using RPC, but <strong>only from allowed endpoints</strong>.</p> <hr/> <h2 id="mining">Mining</h2> <p>Mining can be done through simple scripts. Given the network’s difficulty of <strong>1</strong>, block discovery is almost instantaneous.</p> <p>Although a basic Python mining script is available in the repository, we replaced it with a compact little <a href="https://bitaxe.org/"><strong>ASIC miner</strong></a> for more realistic testing.</p> <hr/> <h2 id="add-ons">Add-ons</h2> <p>Just like in the real Bitcoin network, additional services can be integrated.</p> <h3 id="faucet">Faucet</h3> <p>We implemented a lightweight server running to distribute free coins for testing.</p> <h3 id="block-explorer">Block Explorer</h3> <p>A local Dockerized deployment of <strong>Mempool Space</strong> provides full visibility into blocks and transactions.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/byom/block_explorer-480.webp 480w,/assets/img/posts/byom/block_explorer-800.webp 800w,/assets/img/posts/byom/block_explorer-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/posts/byom/block_explorer.png" class="img-fluid w-75 d-block mx-auto rounded z-depth-1" width="100%" height="auto" alt="Mempool Space local deployment" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Figure 3. Local Mempool Space instance.</figcaption> </figure> <hr/> <h2 id="installation-guide">Installation guide</h2> <p>Alright, the real fun begins now. How do we build it and actually use it? You’ve got a detailed guide over on the GitHub README, but let me break it down for you here.</p> <h3 id="prerequisites">Prerequisites</h3> <p>You’ll need Docker (v20.10 or higher recommended) installed on your system.</p> <h3 id="method-1-download-the-image-from-github-container-registry">Method 1: Download the image from GitHub Container Registry</h3> <p>You can directly pull the pre-built Docker image and run the setup script without building the image from source following these steps:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Pull the image from the GitHub Container Registry</span>
docker pull ghcr.io/dmenec/mainnet_testbed:1.0

<span class="c"># Clone the repository</span>
git clone https://github.com/Dmenec/BTC-Labnet.git
<span class="nb">cd </span>BTC-Labnet

<span class="c"># Run the script to set up Docker containers</span>
./start.sh
</code></pre></div></div> <h3 id="method-2-build-from-source">Method 2: Build from source</h3> <p>Follow these steps to build the image from source:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Clone the repository</span>
git clone https://github.com/Dmenec/BTC-Labnet.git
<span class="nb">cd </span>BTC-Labnet

<span class="c"># Run the script to set up Docker containers</span>
./start.sh
</code></pre></div></div> <p>After execution, three interconnected Bitcoin nodes (bc-node1, bc-node2, and bc-node3) will be created.</p> <hr/> <h2 id="setup-process-details">Setup process details</h2> <h3 id="building-a-customized-docker-image-for-bitcoin-core">Building a customized Docker image for Bitcoin Core</h3> <p>First, modify Bitcoin Core to adjust the code and enable SegWit and Taproot. Then, create a Dockerfile using your own configuration to build your custom Bitcoin Core image. Finally, build the Docker image by running:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker build <span class="nt">-t</span> bitcoin <span class="nb">.</span>
</code></pre></div></div> <h3 id="setting-up-the-docker-network">Setting up the Docker network</h3> <p>Create a Docker bridge network to isolate your Bitcoin nodes:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker network create <span class="nt">-d</span> bridge bitcoin_network
</code></pre></div></div> <h3 id="running-bitcoin-nodes">Running Bitcoin nodes</h3> <p>Start your Bitcoin node with the following command:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker run <span class="nt">-it</span> <span class="nt">-d</span> <span class="nt">--network</span> bitcoin_network <span class="nt">--name</span> bc-node1 <span class="nt">-p</span> 8333:8333 <span class="nt">-p</span> 8332:8332 bitcoin <span class="nt">-printtoconsole</span> <span class="nt">-txindex</span><span class="o">=</span>1 <span class="nt">-reindex</span>
</code></pre></div></div> <hr/> <p>As you can see, installing it isn’t too complicated. While the build does compile Bitcoin Core, so it might take a few minutes, once it’s done… you’ll have all the power to do whatever you want with your Bitcoin blockchain clone!</p> <figure> <picture> <source class="responsive-img-srcset" srcset="https://media4.giphy.com/media/v1.Y2lkPTc5MGI3NjExa3ZqbXIwMDV3cmRybWtqNnVqdDFxOHZyY2JwNHYwbHI1NGI3M2ZjZCZlcD12MV9pbnRlcm5hbF9naWZfYnlfaWQmY3Q9Zw/l36kU80xPf0ojG0Erg/giphy-480.webp 480w,https://media4.giphy.com/media/v1.Y2lkPTc5MGI3NjExa3ZqbXIwMDV3cmRybWtqNnVqdDFxOHZyY2JwNHYwbHI1NGI3M2ZjZCZlcD12MV9pbnRlcm5hbF9naWZfYnlfaWQmY3Q9Zw/l36kU80xPf0ojG0Erg/giphy-800.webp 800w,https://media4.giphy.com/media/v1.Y2lkPTc5MGI3NjExa3ZqbXIwMDV3cmRybWtqNnVqdDFxOHZyY2JwNHYwbHI1NGI3M2ZjZCZlcD12MV9pbnRlcm5hbF9naWZfYnlfaWQmY3Q9Zw/l36kU80xPf0ojG0Erg/giphy-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="https://media4.giphy.com/media/v1.Y2lkPTc5MGI3NjExa3ZqbXIwMDV3cmRybWtqNnVqdDFxOHZyY2JwNHYwbHI1NGI3M2ZjZCZlcD12MV9pbnRlcm5hbF9naWZfYnlfaWQmY3Q9Zw/l36kU80xPf0ojG0Erg/giphy.gif" class="img-fluid w-75 d-block mx-auto rounded z-depth-1" width="100%" height="auto" alt="With great power" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure>]]></content><author><name></name></author><category term="Bitcoin,"/><category term="Docker,"/><category term="Testing,"/><category term="Core,"/><category term="DevOps"/><summary type="html"><![CDATA[How we built a fully isolated Bitcoin mainnet replica in Docker — same genesis block as the real network, zero-cost transactions, and hardware wallet support out of the box.]]></summary></entry></feed>